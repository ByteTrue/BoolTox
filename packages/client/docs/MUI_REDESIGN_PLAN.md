# BoolTox Client - 完整 Material Design 重设计方案

> 从组件迁移到设计语言统一的激进重构计划

**创建时间**: 2025-12-23
**状态**: 待实施
**预计工期**: 6-8 天
**影响范围**: 前端 UI 全面重构

---

## 📋 目录

- [1. 项目概述](#1-项目概述)
- [2. 核心原则](#2-核心原则)
- [3. 现状分析](#3-现状分析)
- [4. 实施路线图](#4-实施路线图)
- [5. 详细实施计划](#5-详细实施计划)
- [6. 文件清单](#6-文件清单)
- [7. 验收标准](#7-验收标准)
- [8. 风险评估](#8-风险评估)
- [9. Git 分支策略](#9-git-分支策略)
- [10. 进度追踪](#10-进度追踪)

---

## 1. 项目概述

### 1.1 背景

当前状态：已完成 MUI 组件迁移（Phase 1-5），但存在以下问题：
- ✅ 组件已替换为 MUI（Button、TextField、Dialog 等）
- ⚠️ 布局思维仍是旧的（Grid 硬编码、Tailwind 残留）
- ⚠️ 数据结构混乱（ModuleGrid 接受 3 种类型）
- ⚠️ 未完全遵循 Material Design 规范

### 1.2 目标

**核心目标**：从"换组件"升级到"用设计语言"

1. **数据结构优先** - 统一模块类型，引入 ViewModel 层
2. **彻底删除 Tailwind** - 100% 使用 MUI sx prop
3. **首页重设计** - 完全符合 Material Dashboard 规范
4. **工具列表重设计** - 统一为 List 视图，优化信息密度
5. **响应式标准化** - 桌面 + 移动端统一体验

### 1.3 Linus 三问答案

```text
Q1: "这是真实问题还是想象的问题?"
A: 真实问题。代码库存在 Tailwind 残留、硬编码断点、类型判断混乱。

Q2: "有更简单的方式吗?"
A: 没有。既然是开发阶段无用户成本，推倒重来比修修补补更干净。

Q3: "这会破坏什么?"
A: 会破坏现有布局，但无用户成本（开发阶段）。
```

---

## 2. 核心原则

### 2.1 设计原则

**【Material Design 规范】**
- 遵循 8dp Grid 系统
- 使用标准 elevation 层级（0/1/2/4/6/8/12/16/24）
- 使用 MUI 默认 transitions 和 easing
- 组件间距使用 `theme.spacing(n)`

**【组件选型原则】**
- 有主/次操作的列表 → `List` + `ListItemButton`
- 纯展示的卡片 → `Card` + `CardContent`
- 导航抽屉 → `Drawer` (persistent/temporary)
- 顶部导航 → `AppBar` + `Toolbar`

### 2.2 代码原则（Linus 风格）

**【Good Taste】**
```typescript
// ✅ 消除特殊情况 - 统一数据类型
const viewModels = modules.map(toViewModel);

// ✅ 简化状态机 - 2 种基本状态
type State = { phase: 'idle' } | { phase: 'active', pid: number };

// ✅ 用配置替代硬编码
gridTemplateColumns: getGridColumns(GRID_BREAKPOINTS.MODULE_CARD)
```

**【Bad Taste】**
```typescript
// ❌ 类型判断地狱
if (isType1(x)) { } else if (isType2(x)) { } else { }

// ❌ Magic numbers
gridTemplateColumns: { xs: '1fr', md: 'repeat(2, 1fr)', lg: 'repeat(3, 1fr)' }

// ❌ 混用 Tailwind + MUI
<Box sx={{ p: 2 }} className="flex items-center gap-4">
```

### 2.3 实施原则

1. **数据结构优先** - 先修数据层，再改 UI
2. **分阶段交付** - 每个 Phase 独立可测试
3. **零回退** - 不允许保留旧代码"以防万一"
4. **统一风格** - 100% MUI，0% Tailwind

---

## 3. 现状分析

### 3.1 技术债清单

| 问题 | 影响文件 | 严重性 | 工作量 |
|------|---------|-------|--------|
| **Tailwind 残留** | `globals.css`, `layout.tsx`, `tool-sources-page.tsx` 等 12+ 文件 | P0 | 1 天 |
| **Grid 硬编码** | `home-page.tsx`, `module-grid.tsx` | P1 | 0.5 天 |
| **类型判断混乱** | `module-grid.tsx` (78-206 行) | P0 | 1 天 |
| **状态机复杂** | `module-card.tsx` (7 种状态) | P1 | 0.5 天 |
| **布局不符合规范** | `home-page.tsx` (无 AppBar) | P2 | 2 天 |

### 3.2 组件架构现状

```
packages/client/src/renderer/
├── pages/
│   ├── home-page.tsx           ⚠️ 需重排布局
│   ├── tools-page.tsx          ✅ 包装器，无需改动
│   ├── settings-page.tsx       ✅ 已符合 Material Design
│   └── tool-sources-page.tsx   ⚠️ 大量 Tailwind 残留
├── components/
│   ├── module-center/
│   │   ├── index.tsx           ⚠️ 状态管理复杂
│   │   ├── module-grid.tsx     ❌ 类型判断混乱
│   │   ├── module-card.tsx     ⚠️ 状态机复杂
│   │   └── module-sidebar.tsx  ⚠️ Tailwind 残留
│   ├── ui/
│   │   ├── activity-feed.tsx   ✅ 已用 MUI
│   │   └── ...                 ✅ 大部分已迁移
│   └── layout.tsx              ⚠️ Tailwind 残留
└── theme/
    ├── mui-theme.ts            ✅ 主题定义完整
    └── theme-provider.tsx      ✅ 状态管理正确
```

### 3.3 关键指标

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| MUI 组件使用率 | 70% | 100% |
| Tailwind 残留文件数 | 12+ | 0 |
| 硬编码响应式断点数 | 3+ | 0 |
| 类型判断分支数 | 5+ | 1 |
| 平均组件复杂度 | 中 | 低 |

---

## 4. 实施路线图

### 4.1 整体时间线

```
Week 1                    Week 2
├──────┬──────┬──────┬────────┬──────┬──────┬──────┤
Day 1  Day 2  Day 3  Day 4    Day 5  Day 6  Day 7  Day 8
  │      │      │       │        │      │      │      │
Phase 0  │   Phase 1   │     Phase 2   │   Phase 3   │
  │      │      │       │        │      │      │      │
 数据层  │  Tailwind   │      首页     │   工具列表  │
 重构    │   清理      │     重设计    │   重设计    │
```

### 4.2 里程碑

| 里程碑 | 完成标志 | 预计完成 |
|--------|---------|----------|
| **M1: 数据层统一** | ViewModel 层完成，类型判断统一 | Day 1 |
| **M2: Tailwind 清零** | 无 Tailwind 残留，100% MUI | Day 3 |
| **M3: 首页重设计** | AppBar + 新布局上线 | Day 5 |
| **M4: 工具列表重设计** | List 视图 + 响应式 Drawer | Day 7 |
| **M5: 验收通过** | 所有测试通过，代码审查完成 | Day 8 |

---

## 5. 详细实施计划

### Phase 0: 数据结构重构（Day 1，6-8 小时）

#### 5.0.1 目标
统一模块数据类型，消除类型判断地狱。

#### 5.0.2 任务清单

**Task 0.1: 创建 ViewModel 层**
- [ ] 新建 `module-view-model.ts`
- [ ] 定义统一的 `ModuleViewModel` 接口
- [ ] 实现 `toModuleViewModel()` 工厂函数
- [ ] 导出类型守卫函数（如需要）

**Task 0.2: 重构 ModuleGrid**
- [ ] 删除 78-88 行的类型判断函数
- [ ] 删除 101-206 行的嵌套 if/else
- [ ] 使用 `viewModels.map()` 统一渲染
- [ ] 更新 Props 类型定义

**Task 0.3: 更新相关组件**
- [ ] `ModuleCard.tsx` 改为接受 `ModuleViewModel`
- [ ] `ModuleListItem.tsx` 改为接受 `ModuleViewModel`
- [ ] `AvailableModuleCard.tsx` 简化 Props

**Task 0.4: 简化状态机**
- [ ] `module-card.tsx` 状态从 7 种简化为 2 种
- [ ] 删除临时状态（launching/stopping 改为 UI loading）

#### 5.0.3 验收标准
- [ ] `ModuleGrid` 代码行数减少 50%+
- [ ] 无类型错误
- [ ] 所有模块正常显示（已安装/可用/远程）
- [ ] 单元测试通过（如有）

#### 5.0.4 关键文件
- 新建: `packages/client/src/renderer/components/module-center/module-view-model.ts`
- 修改: `module-grid.tsx`, `module-card.tsx`, `module-list-item.tsx`

---

### Phase 1: Tailwind 完全清除（Day 2-3，8-12 小时）

#### 5.1.1 目标
删除所有 Tailwind 引用，统一为 MUI `sx` prop。

#### 5.1.2 任务清单

**Task 1.1: 删除 Tailwind 配置**
- [ ] `globals.css` 删除第 1-4 行的 `@import 'tailwindcss'`
- [ ] 保留 CSS 变量和自定义动画
- [ ] `package.json` 删除 tailwindcss 依赖
- [ ] 运行 `pnpm remove tailwindcss @tailwindcss/typography`

**Task 1.2: 清理核心布局**
- [ ] `layout.tsx` 第 60 行改用 MUI `Box`
- [ ] 删除 `className="antialiased bg-[var(--shell-background)]"`
- [ ] 改用 `sx={{ WebkitFontSmoothing: 'antialiased', bgcolor: 'background.default' }}`

**Task 1.3: 首页响应式修复**
- [ ] 新建 `theme/grid-config.ts` 配置文件
- [ ] 定义 `GRID_BREAKPOINTS` 常量
- [ ] 实现 `getGridColumns()` 辅助函数
- [ ] `home-page.tsx` 第 82-87 行改用 MUI Grid2

**Task 1.4: ModuleGrid 响应式修复**
- [ ] `module-grid.tsx` 导入 grid-config
- [ ] 第 55-60 行改用 `getGridColumns()`
- [ ] 第 96-98 行同样修改

**Task 1.5: 清理残留页面**
- [ ] `tool-sources-page.tsx` 清理 30+ 处 Tailwind 类
- [ ] `add-tool-source-page.tsx` 清理 20+ 处 Tailwind 类
- [ ] `settings/about.tsx` 清理 5+ 处 Tailwind 类
- [ ] 其他页面扫描清理

**Task 1.6: ModuleSidebar Tailwind 清理**
- [ ] `module-sidebar.tsx` 删除所有 `className`
- [ ] 改用 MUI `List` + `ListItemButton`
- [ ] 保留 `.elegant-scroll` 全局类（如需要）

#### 5.1.3 验收标准
- [ ] `rg "className.*=" --type tsx | grep -E "(bg-|text-|flex|grid|p-|m-)"` 返回 0 结果
- [ ] `pnpm dev` 无报错
- [ ] 所有页面视觉正常
- [ ] `pnpm build` 成功

#### 5.1.4 关键文件
- 新建: `theme/grid-config.ts`
- 修改: `globals.css`, `layout.tsx`, `home-page.tsx`, `module-grid.tsx`, `module-sidebar.tsx`, `tool-sources-page.tsx`, `add-tool-source-page.tsx`
- 删除: 无

---

### Phase 2: 首页 Material Dashboard 重设计（Day 4-5，10-12 小时）

#### 5.2.1 目标
首页完全重排，符合 Material Design Dashboard 规范。

#### 5.2.2 新设计稿（文字描述）

```
┌────────────────────────────────────────────────────────────┐
│ [AppBar]                                                    │
│   🏠 BoolTox    [搜索框]           [通知] [设置] [用户]    │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 晚上好 👋                            [统计卡片 x3]   │ │
│  │ 12月23日 周二                                         │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                             │
│  💡 快速启动                                                │
│  ┌────────────┬────────────┬────────────┬────────────┐   │
│  │ [工具 1]   │ [工具 2]   │ [工具 3]   │ [更多...]  │   │
│  │  图标       │  图标       │  图标       │            │   │
│  │  名称       │  名称       │  名称       │            │   │
│  │ [启动]      │ [启动]      │ [启动]      │            │   │
│  └────────────┴────────────┴────────────┴────────────┘   │
│                                                             │
│  ┌─────────────────────────┬─────────────────────────┐   │
│  │ 📣 系统公告              │ 📝 最近活动              │   │
│  │                         │                         │   │
│  │ [公告内容]              │ [活动时间线]            │   │
│  │                         │                         │   │
│  └─────────────────────────┴─────────────────────────┘   │
│                                                             │
│  💻 系统监控                                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ CPU: 45%  内存: 8.2GB/16GB  磁盘: 512GB/1TB          │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

#### 5.2.3 任务清单

**Task 2.1: 新建 AppBar 组件**
- [ ] 新建 `components/app-bar.tsx`
- [ ] 实现顶部导航栏（Logo + 搜索 + 操作按钮）
- [ ] 集成到 `app-shell.tsx`
- [ ] 处理高度（TabBar 与 AppBar 共存问题）

**Task 2.2: 重构 Hero 区域**
- [ ] `home-page.tsx` 第 38-63 行改为 Card 风格
- [ ] 统计卡片移到右侧（响应式布局）
- [ ] 使用 MUI Grid2 布局

**Task 2.3: 快速启动区域重设计**
- [ ] "最近使用"改为 Grid 卡片（不再是横向滚动）
- [ ] 使用 `Grid container spacing={2}`
- [ ] 每个工具显示为 Mini Card（图标 + 名称 + 启动按钮）

**Task 2.4: 双栏布局优化**
- [ ] 公告和活动记录改用 MUI Grid2
- [ ] 响应式：移动端改为单栏

**Task 2.5: 系统监控优化**
- [ ] 保持当前 `SystemMonitor` 组件
- [ ] 调整间距和圆角

#### 5.2.4 验收标准
- [ ] 首页有 AppBar
- [ ] Hero 区域左右布局（问候 + 统计卡片）
- [ ] 快速启动为 Grid 卡片（不是横向滚动）
- [ ] 响应式：移动端正常显示
- [ ] 视觉符合 Material Design

#### 5.2.5 关键文件
- 新建: `components/app-bar.tsx`
- 修改: `home-page.tsx`, `app-shell.tsx`

---

### Phase 3: 工具列表 List Only 重设计（Day 6-7，10-14 小时）

#### 5.3.1 目标
工具列表统一为 List 视图，优化信息密度，侧边栏响应式。

#### 5.3.2 新设计稿（文字描述）

```
桌面端:
┌─────────┬──────────────────────────────────────────────┐
│[侧边栏] │ [顶部工具栏]                                  │
│         │  [搜索] [排序] [+ 本地工具]                  │
│ 已安装  ├──────────────────────────────────────────────┤
│ 收藏    │ 📦 已安装工具 (8 个项目)                     │
│ 运行中  │                                               │
│         │ ┌────────────────────────────────────────┐   │
│ 工具源  │ │ 🔧 密码生成器             [打开] [📌] [🗑] │   │
│ ├─官方  │ │ v2.0.0 · utilities · 6天前              │   │
│ └─社区  │ │ 专业的密码生成工具，支持自定义规则...   │   │
│         │ └────────────────────────────────────────┘   │
│ 分类    │                                               │
│ ├─全部  │ ┌────────────────────────────────────────┐   │
│ ├─工具  │ │ 🎨 UI Auto Dev                [停止] [...] │   │
│ └─开发  │ │ v1.0.0 · frontend · 运行中             │   │
└─────────┤ │ 自动化前端开发工具，快速生成组件...    │   │
          │ └────────────────────────────────────────┘   │
          └──────────────────────────────────────────────┘

移动端:
┌──────────────────────────────────────┐
│ [☰] 已安装工具           [搜索] [+]  │
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │
│ │ 🔧 密码生成器         [打开] [⋮] │ │
│ │ v2.0.0 · 6天前                   │ │
│ └──────────────────────────────────┘ │
│                                      │
│ ┌──────────────────────────────────┐ │
│ │ 🎨 UI Auto Dev        [停止] [⋮] │ │
│ │ v1.0.0 · 运行中                  │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

#### 5.3.3 任务清单

**Task 3.1: 删除 Grid 视图模式**
- [ ] `module-center/index.tsx` 删除 `viewMode` 状态
- [ ] 删除 Grid/List 切换按钮（ToggleButtonGroup）
- [ ] `module-grid.tsx` 重命名为 `module-list.tsx`
- [ ] 删除 Grid 相关逻辑

**Task 3.2: 优化 List 信息密度**
- [ ] `ModuleListItem.tsx` 重新设计布局
- [ ] 左侧：图标（48x48）+ 名称 + 版本 + 标签 + 描述（2 行）
- [ ] 右侧：主操作按钮 + 次要操作（收藏/卸载）
- [ ] 使用 `ListItemButton` 实现点击展开详情

**Task 3.3: 响应式侧边栏**
- [ ] `module-sidebar.tsx` 改为 MUI Drawer
- [ ] 桌面端：`variant="persistent"` 始终显示
- [ ] 移动端：`variant="temporary"` 点击展开
- [ ] 添加折叠/展开按钮

**Task 3.4: AvailableModuleCard 改为 ListItem**
- [ ] 未安装工具也使用 List 展示
- [ ] 与已安装工具样式一致
- [ ] 主操作按钮改为"安装"

**Task 3.5: 顶部工具栏调整**
- [ ] 删除视图切换按钮
- [ ] 调整搜索框宽度（更宽）
- [ ] 移动端优化布局

#### 5.3.4 验收标准
- [ ] 无 Grid 视图模式
- [ ] 所有模块以 List 展示
- [ ] List 信息密度合理（每屏 6-8 个）
- [ ] 桌面端侧边栏始终显示
- [ ] 移动端侧边栏可折叠
- [ ] 视觉符合 Material Design List 规范

#### 5.3.5 关键文件
- 重命名: `module-grid.tsx` → `module-list.tsx`
- 修改: `module-center/index.tsx`, `module-list-item.tsx`, `module-sidebar.tsx`
- 删除: 无（Grid 相关逻辑内联删除）

---

## 6. 文件清单

### 6.1 新建文件

| 文件路径 | 说明 | 阶段 |
|---------|------|------|
| `components/module-center/module-view-model.ts` | ViewModel 层 | Phase 0 |
| `theme/grid-config.ts` | Grid 响应式配置 | Phase 1 |
| `components/app-bar.tsx` | 顶部导航栏 | Phase 2 |

### 6.2 修改文件（核心）

| 文件路径 | 修改范围 | 阶段 |
|---------|---------|------|
| `components/module-center/module-grid.tsx` → `module-list.tsx` | 全面重构 | Phase 0, 3 |
| `components/module-center/module-card.tsx` | 状态机简化 | Phase 0 |
| `components/module-center/module-list-item.tsx` | 布局优化 | Phase 3 |
| `components/module-center/module-sidebar.tsx` | Tailwind 清理 + Drawer | Phase 1, 3 |
| `components/module-center/index.tsx` | 删除 Grid 逻辑 | Phase 3 |
| `pages/home-page.tsx` | 完全重排 | Phase 1, 2 |
| `pages/tool-sources-page.tsx` | Tailwind 清理 | Phase 1 |
| `pages/add-tool-source-page.tsx` | Tailwind 清理 | Phase 1 |
| `components/layout.tsx` | Tailwind 清理 | Phase 1 |
| `globals.css` | 删除 Tailwind 导入 | Phase 1 |
| `app-shell.tsx` | 集成 AppBar | Phase 2 |

### 6.3 删除文件

| 文件路径 | 原因 | 阶段 |
|---------|------|------|
| 无 | Grid 逻辑内联删除，不涉及整文件删除 | - |

### 6.4 修改统计

| 类型 | 数量 | 估算行数变化 |
|------|------|-------------|
| 新建文件 | 3 | +400 行 |
| 修改文件 | 12 | +800 / -1200 |
| 删除文件 | 0 | 0 |
| **净变化** | - | **-400 行（代码更简洁）** |

---

## 7. 验收标准

### 7.1 功能验收

**Phase 0 验收**
- [ ] `ModuleGrid` 不再有类型判断逻辑
- [ ] 所有模块类型（已安装/可用/远程）正常显示
- [ ] 单元测试通过（如有）

**Phase 1 验收**
- [ ] 无 Tailwind 残留（`rg "className.*=" | grep Tailwind` 返回 0）
- [ ] 所有页面视觉正常
- [ ] `pnpm build` 成功
- [ ] 无 console 报错

**Phase 2 验收**
- [ ] 首页有 AppBar
- [ ] Hero 区域左右布局
- [ ] 快速启动为 Grid 卡片
- [ ] 响应式正常（桌面/平板/移动）

**Phase 3 验收**
- [ ] 无 Grid 视图切换按钮
- [ ] 所有模块以 List 展示
- [ ] 侧边栏响应式（桌面固定/移动折叠）
- [ ] List 信息密度合理

### 7.2 视觉验收

**Material Design 规范检查**
- [ ] 使用 8dp Grid 系统
- [ ] elevation 符合规范（Paper: 1, Card: 2, Dialog: 24）
- [ ] 圆角统一（Button: 8px, Card: 16px, Dialog: 16px）
- [ ] 间距统一使用 `theme.spacing(n)`
- [ ] 颜色使用主题 palette

**响应式检查**
- [ ] 桌面端（1920x1080）布局合理
- [ ] 平板端（768x1024）布局合理
- [ ] 移动端（375x667）布局合理
- [ ] 无横向滚动条
- [ ] 触摸目标 ≥ 48x48 dp

### 7.3 性能验收

- [ ] 首屏加载时间 < 2s
- [ ] 页面切换流畅（无卡顿）
- [ ] 无内存泄漏（长时间运行测试）
- [ ] 构建产物大小合理（无明显增加）

### 7.4 代码质量验收

- [ ] ESLint 无错误
- [ ] Prettier 格式检查通过
- [ ] TypeScript 无类型错误
- [ ] 代码审查通过（如有团队）

---

## 8. 风险评估

### 8.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **类型重构破坏运行时逻辑** | 中 | 高 | Phase 0 完成后全面测试 3 种模块类型 |
| **删除 Grid 视图后信息密度不足** | 中 | 中 | List 设计时优化布局，确保每屏 6-8 个 |
| **响应式 Drawer 在移动端性能差** | 低 | 中 | 使用 `Drawer` 的 `disableScrollLock` prop |
| **AppBar 与 TabBar 高度冲突** | 中 | 中 | 计算总高度，调整 content 区域 padding |
| **Tailwind 删除后样式丢失** | 低 | 高 | Phase 1 前截图对比，逐页验证 |

### 8.2 进度风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **Phase 0 耗时超预期** | 中 | 中 | 优先完成核心逻辑，边缘情况可后补 |
| **Phase 3 设计迭代多** | 高 | 中 | 提前与产品/设计确认 List 布局 |
| **测试发现重大 bug** | 中 | 高 | 每个 Phase 完成后充分测试再继续 |

### 8.3 回滚策略

**Phase 级别回滚**
```bash
# 回滚到 Phase N 完成时的状态
git checkout phase-N-stable
git checkout -b phase-N-fix
# 修复问题后重新尝试
```

**紧急回滚（回到 refactor/ui 分支起点）**
```bash
git checkout refactor/ui
git reset --hard <起始commit>
```

---

## 9. Git 分支策略

### 9.1 分支结构

```
main (生产稳定)
 │
 └── refactor/ui (重构主分支)
      │
      ├── phase-0/data-layer (Phase 0)
      │    └── PR #1 → refactor/ui
      │
      ├── phase-1/cleanup-tailwind (Phase 1)
      │    └── PR #2 → refactor/ui
      │
      ├── phase-2/homepage-redesign (Phase 2)
      │    └── PR #3 → refactor/ui
      │
      └── phase-3/list-only (Phase 3)
           └── PR #4 → refactor/ui
```

### 9.2 分支命名规范

```bash
phase-<N>/<功能描述>

例如:
phase-0/data-layer
phase-1/cleanup-tailwind
phase-2/homepage-redesign
phase-3/list-only
```

### 9.3 Commit 规范

```bash
<type>(<scope>): <subject>

type:
- feat: 新功能
- refactor: 重构
- fix: 修复
- style: 样式调整
- chore: 杂项

例如:
feat(module-center): add ViewModel layer
refactor(home-page): redesign Hero section
chore(deps): remove tailwindcss
```

### 9.4 PR 流程

1. **创建 Phase 分支**
   ```bash
   git checkout refactor/ui
   git checkout -b phase-N/<desc>
   ```

2. **完成开发 + 自测**
   ```bash
   git add -A
   git commit -m "feat(xxx): ..."
   pnpm test  # 如有测试
   pnpm build  # 确保构建成功
   ```

3. **提交 PR**
   - 标题: `[Phase N] <功能描述>`
   - 描述: 包含任务清单和截图
   - 审查: 自审 + 代码审查（如有团队）

4. **合并后打标签**
   ```bash
   git tag phase-N-stable
   git push origin phase-N-stable
   ```

---

## 10. 进度追踪

### 10.1 总体进度

| 阶段 | 预计工期 | 开始日期 | 完成日期 | 状态 |
|------|---------|---------|---------|------|
| **Phase 0** | 1 天 | - | - | ⏸️ 待开始 |
| **Phase 1** | 2 天 | - | - | ⏸️ 待开始 |
| **Phase 2** | 2 天 | - | - | ⏸️ 待开始 |
| **Phase 3** | 2 天 | - | - | ⏸️ 待开始 |
| **验收测试** | 1 天 | - | - | ⏸️ 待开始 |

### 10.2 Phase 0 任务追踪

- [ ] Task 0.1: 创建 ViewModel 层
- [ ] Task 0.2: 重构 ModuleGrid
- [ ] Task 0.3: 更新相关组件
- [ ] Task 0.4: 简化状态机
- [ ] ✅ Phase 0 验收通过

### 10.3 Phase 1 任务追踪

- [ ] Task 1.1: 删除 Tailwind 配置
- [ ] Task 1.2: 清理核心布局
- [ ] Task 1.3: 首页响应式修复
- [ ] Task 1.4: ModuleGrid 响应式修复
- [ ] Task 1.5: 清理残留页面
- [ ] Task 1.6: ModuleSidebar Tailwind 清理
- [ ] ✅ Phase 1 验收通过

### 10.4 Phase 2 任务追踪

- [ ] Task 2.1: 新建 AppBar 组件
- [ ] Task 2.2: 重构 Hero 区域
- [ ] Task 2.3: 快速启动区域重设计
- [ ] Task 2.4: 双栏布局优化
- [ ] Task 2.5: 系统监控优化
- [ ] ✅ Phase 2 验收通过

### 10.5 Phase 3 任务追踪

- [ ] Task 3.1: 删除 Grid 视图模式
- [ ] Task 3.2: 优化 List 信息密度
- [ ] Task 3.3: 响应式侧边栏
- [ ] Task 3.4: AvailableModuleCard 改为 ListItem
- [ ] Task 3.5: 顶部工具栏调整
- [ ] ✅ Phase 3 验收通过

### 10.6 每日进度记录

**Day 1 (Phase 0)**
- [ ] 09:00-10:30 创建 ViewModel 层
- [ ] 10:30-12:00 重构 ModuleGrid
- [ ] 14:00-16:00 更新相关组件
- [ ] 16:00-17:30 简化状态机
- [ ] 17:30-18:00 自测 + 验收

**Day 2-3 (Phase 1)**
- [ ] Day 2: Task 1.1-1.3
- [ ] Day 3: Task 1.4-1.6 + 验收

**Day 4-5 (Phase 2)**
- [ ] Day 4: Task 2.1-2.3
- [ ] Day 5: Task 2.4-2.5 + 验收

**Day 6-7 (Phase 3)**
- [ ] Day 6: Task 3.1-3.3
- [ ] Day 7: Task 3.4-3.5 + 验收

**Day 8 (最终验收)**
- [ ] 全面功能测试
- [ ] 响应式测试
- [ ] 性能测试
- [ ] 代码审查
- [ ] 文档更新

---

## 附录 A: 快速命令参考

### 开发命令
```bash
# 启动开发服务器
pnpm --filter @booltox/client dev

# 构建
pnpm --filter @booltox/client build

# 类型检查
pnpm --filter @booltox/client type-check

# Lint
pnpm --filter @booltox/client lint

# 格式化
pnpm --filter @booltox/client format
```

### 检查命令
```bash
# 检查 Tailwind 残留
rg "className.*=" --type tsx --glob "!node_modules" | grep -E "(bg-|text-|flex|grid|p-|m-)"

# 检查硬编码响应式断点
rg "gridTemplateColumns.*xs.*md.*lg" --type tsx

# 统计组件复杂度
cd packages/client/src/renderer/components
find . -name "*.tsx" -exec wc -l {} \; | sort -rn | head -20
```

### Git 命令
```bash
# 创建 Phase 分支
git checkout refactor/ui
git checkout -b phase-N/<desc>

# 提交
git add -A
git commit -m "feat(scope): description"

# 推送
git push origin phase-N/<desc>

# 打标签
git tag phase-N-stable
git push origin phase-N-stable
```

---

## 附录 B: Material Design 参考

### 官方资源
- Material Design 3: https://m3.material.io/
- MUI 文档: https://mui.com/material-ui/
- Material Design Icons: https://fonts.google.com/icons

### 核心规范
- **8dp Grid**: 所有间距为 8 的倍数（4/8/12/16/24/32）
- **Elevation**: 0/1/2/4/6/8/12/16/24
- **Border Radius**: 4/8/12/16/20/24
- **Typography Scale**: h1-h6, body1-body2, caption, button

---

## 附录 C: 联系与支持

**文档维护者**: Feature-dev Agent
**创建日期**: 2025-12-23
**最后更新**: 2025-12-23
**版本**: v1.0.0

**问题反馈**:
- 如有疑问或建议，请在项目 Issue 中提出
- 实施过程中遇到问题，及时更新本文档

---

**Linus 最后的话**:

> "Data structures, not code, should be your focus. Get the data model right, and the code writes itself.
> Delete code aggressively. Every line of code is a liability, not an asset.
> And remember: 'Good taste' means eliminating special cases, not handling them elegantly."

**祝重构顺利！🚀**
