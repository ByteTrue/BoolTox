# 快捷面板调试指南

> 创建时间：2025-12-15

---

## 🔍 问题诊断

### 问题现象
按 `Ctrl+Shift+Space` 无法唤出快捷面板

### 已修复的问题
1. ✅ 路由处理缺失（`app-shell.tsx` 未处理 `#/quick-panel`）
2. ✅ API 调用错误（`window.api.quickPanel` → `window.quickPanel`）

---

## 🔧 调试步骤

### 1. 检查日志
```bash
# 查看快捷面板日志
grep "Quick panel" packages/client/logs/app.*.log
```

**预期输出**：
```
Quick panel window created
Quick panel shortcut registered: Ctrl+Shift+Space
Quick panel IPC handlers registered
```

### 2. 检查快捷键注册
打开开发者工具（主窗口），在控制台运行：
```javascript
// 检查 quickPanel API 是否存在
console.log(window.quickPanel);

// 应该输出：
// {
//   hide: f(),
//   showMain: f(),
//   navigateTo: f(route)
// }
```

### 3. 手动测试快捷面板
在控制台运行：
```javascript
// 手动显示快捷面板（模拟快捷键触发）
window.location.hash = '#/quick-panel';
```

如果快捷面板显示，说明路由正确，问题在快捷键注册。

### 4. 检查快捷键冲突
`Ctrl+Shift+Space` 可能被其他应用占用。

**常见冲突**：
- Windows：输入法快捷键
- 搜狗输入法：`Ctrl+Shift+Space` 切换中英文
- 微信：可能有全局快捷键

**解决方案**：
1. 临时关闭其他应用
2. 修改快捷键为 `Ctrl+Alt+Space`（备用方案）

---

## 🛠️ 修改快捷键（如果冲突）

### 方法 1：修改源码
编辑 `electron/windows/quick-panel-manager.ts:83`：
```typescript
// 改为 Ctrl+Alt+Space
const shortcut = process.platform === 'darwin'
  ? 'Command+Alt+Space'
  : 'Ctrl+Alt+Space';
```

### 方法 2：添加配置项
未来可以在设置页添加自定义快捷键功能。

---

## ✅ 验证清单

- [ ] 日志显示快捷键已注册
- [ ] `window.quickPanel` 存在
- [ ] 手动设置 hash 可以显示快捷面板
- [ ] 按快捷键可以唤出面板
- [ ] ESC 键可以关闭面板
- [ ] 点击工具可以启动并关闭面板

---

## 🎯 下次启动测试

重新编译后测试：
```bash
pnpm dev:client
```

然后按 `Ctrl+Shift+Space`，应该能看到快捷面板弹出！
