# BoolTox Web UI/UX 设计方案

> **版本**: v1.0
> **创建日期**: 2025-12-07
> **设计目标**: 基于 2025 年 Web 设计趋势，打造独特的插件生态平台

---

## 📋 目录

- [设计理念](#设计理念)
- [视觉设计系统](#视觉设计系统)
- [核心页面设计](#核心页面设计)
- [关键组件设计](#关键组件设计)
- [UI 组件库选择](#ui-组件库选择)
- [动效和交互](#动效和交互)
- [2025 趋势应用](#2025-趋势应用)
- [实施路线图](#实施路线图)
- [参考案例](#参考案例)

---

## 🎯 设计理念

### 核心设计哲学："指挥中心"美学

BoolTox Web 不是传统的网页应用，而是一个**效率工具的指挥中心**。设计语言围绕以下核心理念：

1. **命令驱动** - 一切操作都可以通过 ⌘K 完成
2. **插件即卡片** - 每个插件都是独立的视觉单元
3. **状态可视化** - 实时展示 Agent 运行状态
4. **视觉密度可调** - 用户可选择"紧凑"/"舒适"模式

### 灵感来源

- **Arc Browser** - 现代化侧边栏交互
- **Raycast** - 命令优先的工作流
- **Linear** - 极简主义 + 精致微动效
- **Vercel Dashboard** - 数据可视化
- **Figma** - 插件生态展示

---

## 🎨 视觉设计系统

### 配色方案

#### 主色调 - 电光蓝 (Electric Blue)

```css
/* 主色 */
--primary: 210 100% 56%;        /* #0EA5E9 - 科技感 */
--primary-dark: 210 100% 48%;   /* #0284C7 */

/* 辅助色 - 紫罗兰渐变 */
--accent: 270 80% 65%;          /* #A855F7 - 高级功能 */

/* 语义色 */
--success: 142 76% 36%;         /* #059669 - 官方插件 */
--warning: 38 92% 50%;          /* #F59E0B - 社区插件 */
--error: 0 84% 60%;             /* #EF4444 */

/* 中性色 - 深空灰 */
/* Light Mode */
--bg-primary: 0 0% 100%;        /* 纯白 */
--bg-secondary: 240 5% 96%;     /* #F5F5F7 */
--text-primary: 240 10% 4%;     /* #0A0A0A */

/* Dark Mode */
--bg-primary: 240 10% 4%;       /* #0A0A0A - 深空黑 */
--bg-secondary: 240 8% 8%;      /* #141414 */
--text-primary: 0 0% 98%;       /* #FAFAFA */
```

#### 特殊效果色

```css
/* 霓虹渐变 - CTA按钮 */
background: linear-gradient(135deg, #0EA5E9 0%, #A855F7 100%);

/* 玻璃态 */
background: rgba(255, 255, 255, 0.1);
backdrop-filter: blur(12px) saturate(180%);
border: 1px solid rgba(255, 255, 255, 0.2);
```

---

### 字体系统

```css
/* 主字体 */
font-family-sans: 'Inter Variable', 'SF Pro Display', 'Noto Sans SC', sans-serif;

/* 等宽字体（代码） */
font-family-mono: 'JetBrains Mono', 'Fira Code', monospace;

/* 字号阶梯（1.250 Major Third Scale）*/
--text-xs: 0.75rem;     /* 12px - 辅助信息 */
--text-sm: 0.875rem;    /* 14px - 次要文字 */
--text-base: 1rem;      /* 16px - 正文 */
--text-lg: 1.25rem;     /* 20px - 小标题 */
--text-xl: 1.563rem;    /* 25px - 卡片标题 */
--text-2xl: 1.953rem;   /* 31px - 页面标题 */
--text-3xl: 2.441rem;   /* 39px - Hero标题 */
--text-4xl: 3.052rem;   /* 49px - 首页大标题 */
```

---

### 间距和圆角

#### 间距系统（4px 基础单位）

```css
--space-1: 4px
--space-2: 8px
--space-3: 12px
--space-4: 16px
--space-6: 24px
--space-8: 32px
--space-12: 48px
--space-16: 64px
```

#### 圆角系统

```css
--radius-sm: 4px        /* Badge */
--radius-md: 8px        /* Button */
--radius-lg: 12px       /* Card */
--radius-xl: 16px       /* Large Card */
--radius-2xl: 24px      /* Modal */
--radius-full: 9999px   /* Avatar */
```

---

### 阴影和层级

#### 层级系统（Z-index）

```css
--z-base: 0             /* 普通内容 */
--z-dropdown: 1000      /* 下拉菜单 */
--z-sticky: 1100        /* 固定导航 */
--z-modal: 1200         /* 模态框 */
--z-toast: 1300         /* 通知 */
--z-command: 1400       /* 命令面板 */
```

#### 阴影系统

```css
/* 小阴影 - 悬停卡片 */
--shadow-sm: 0 2px 4px -1px rgb(0 0 0 / 0.06), 0 1px 2px -1px rgb(0 0 0 / 0.1);

/* 中阴影 - 下拉菜单 */
--shadow-md: 0 4px 12px -2px rgb(0 0 0 / 0.08), 0 2px 4px -2px rgb(0 0 0 / 0.12);

/* 大阴影 - 模态框 */
--shadow-lg: 0 20px 40px -8px rgb(0 0 0 / 0.12), 0 8px 16px -4px rgb(0 0 0 / 0.16);

/* 霓虹光晕 - CTA按钮 */
--shadow-glow: 0 8px 24px -4px rgb(14 165 233 / 0.4);
```

---

## 📐 核心页面设计

### 1. 首页 (Landing Page)

**设计目标**: 震撼的第一印象 + 快速理解产品价值

**布局结构**:
```
┌─────────────────────────────────────────────────────────┐
│  [Navbar - 透明渐变背景]                                │
├─────────────────────────────────────────────────────────┤
│              ✨ 开源 · 可扩展 · 完全免费                 │
│                    BoolTox                               │ <- 120px 超大字体
│              ━━━━━━━━━━━                                 │
│         一站式效率工具平台                                │
│     插件生态 · Web + Agent 混合架构                       │
│    [探索工具箱 →]  [浏览插件]                            │
│                                                          │
│  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐               │
│  │ 🔌    │ │ ⚡    │ │ 🛡️    │ │ 🎨    │               │
│  │ 500+  │ │ 原生  │ │ 安全  │ │ 主题  │               │
│  │ 插件  │ │ 性能  │ │ 沙箱  │ │ 定制  │               │
│  └───────┘ └───────┘ └───────┘ └───────┘               │
│                                                          │
│  ━━━━━ 热门插件预览（横向滚动） ━━━━━                    │
└─────────────────────────────────────────────────────────┘
```

**特殊效果**:
- 深色网格背景 + 动态光点（Canvas 粒子）
- 标题渐变色 + 打字机效果
- 特性卡片 3D 倾斜效果（鼠标跟随）
- 插件预览自动横向滚动

---

### 2. 插件市场 (Marketplace)

**设计目标**: 快速浏览 + 精准筛选

**布局结构（双栏）**:
```
┌─────────────────────────────────────────────────────────┐
│ [← 返回]  插件市场                    [⌘K]    [+ 新建]  │
├───────────┬─────────────────────────────────────────────┤
│ 分类      │  [搜索框]  [全部▾] [官方] [社区]            │
│ ━━━━━     │  ─────────────────────────────────────────  │
│ 📊 开发工具│  ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│ 📝 文本处理│  │ 🐍      │ │ 📦      │ │ 🎨      │      │
│ 🎨 设计工具│  │ Python  │ │ Package │ │ Theme   │      │
│ 🔒 安全工具│  │ Runner  │ │ Manager │ │ Editor  │      │
│ 🌐 网络工具│  │ ⭐⭐⭐⭐⭐│ │ ⭐⭐⭐⭐  │ │ ⭐⭐⭐   │      │
│           │  │ 25.8K↓  │ │ 12.3K↓  │ │ 8.1K↓   │      │
│ 标签      │  │ [安装]  │ │ [安装]  │ │ [安装]  │      │
│ #python   │  └─────────┘ └─────────┘ └─────────┘      │
│ #nodejs   │                                             │
└───────────┴─────────────────────────────────────────────┘
```

**关键设计点**:
- 侧边栏固定（240px）
- 插件卡片悬停上浮 + 阴影加深
- 骨架屏加载（非 Spinner）
- 筛选器联动（分类 + 标签）

---

### 3. 插件详情页 (Plugin Detail)

**设计目标**: 沉浸式展示 + 快速决策

**布局结构（App Store 风格）**:
```
┌─────────────────────────────────────────────────────────┐
│ [← 返回]                                                 │
├─────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────┐    │ <- Hero区域
│  │     [大型预览图 / 视频演示]                     │    │    渐变背景
│  └────────────────────────────────────────────────┘    │
│  🐍  Python Runner                  [✓ 已安装]          │
│      ⭐⭐⭐⭐⭐ 4.9 | 25,827↓ | v2.1.0 | MIT          │
│  [打开] [卸载] [GitHub]                                  │
│  ┌───────────────────────────────────────┐             │
│  │ [← screenshot 1/5 →]                  │             │
│  └───────────────────────────────────────┘             │
│  [简介] [功能] [更新日志] [评论(125)]                   │
│  ## 简介                                                 │
│  Python Runner 是...                                     │
└─────────────────────────────────────────────────────────┘
```

**特殊效果**:
- Hero 背景使用插件主题色渐变
- 截图轮播支持触摸滑动
- 安装按钮霓虹光晕 + 进度动画

---

### 4. 工具箱 (Dashboard)

**设计目标**: 效率优先 + 状态可视化

**布局结构**:
```
┌─────────────────────────────────────────────────────────┐
│ [⌘K 搜索工具...]              [网格] [列表]    [+ 新建]   │
├─────────────────────────────────────────────────────────┤
│  已安装工具 (12)           [全部] [运行中] [已停止]      │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐            │
│  │ 🐍 •      │ │ 📦        │ │ 🎨        │            │
│  │ Python    │ │ Package   │ │ Theme     │            │
│  │ Runner    │ │ Manager   │ │ Editor    │            │
│  │ ──────    │ │           │ │           │            │
│  │ ▶ 运行中  │ │ ⏸ 已停止  │ │ ⏸ 已停止  │            │
│  │ CPU: 12%  │ │           │ │           │            │
│  │ MEM: 45MB │ │ 2h前使用   │ │ v1.2.0    │            │
│  │ [停止]    │ │ [启动]    │ │ [启动]    │            │
│  └───────────┘ └───────────┘ └───────────┘            │
│  ┌─────────────────────────────────────────┐           │
│  │ 🤖 Agent 状态                            │           │
│  │ Python 3.11.5  ✓ | Node.js 20.5.0 ✓     │           │
│  └─────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────┘
```

**关键功能**:
- 状态指示器（绿点/灰点/红点）
- CPU/内存实时更新
- 右键菜单快速操作
- 拖拽排序（@dnd-kit）

---

## 🧩 关键组件设计

### 1. 插件卡片 (PluginCard)

```tsx
// 组件结构
<div className="plugin-card">
  <div className="card-header">
    <Badge>官方</Badge>
    <Rating value={4.9} />
  </div>
  <div className="card-icon">
    <Icon size={48} />
  </div>
  <h3 className="card-title">插件名称</h3>
  <p className="card-description">描述...</p>
  <div className="card-tags">
    <Tag>#python</Tag>
    <Tag>#automation</Tag>
  </div>
  <div className="card-footer">
    <Status>运行中</Status>
    <Downloads>25.8K</Downloads>
  </div>
  <div className="card-actions">
    <Button>安装</Button>
    <Button variant="ghost">详情</Button>
  </div>
</div>
```

**CSS 关键点**:
```css
.plugin-card {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.plugin-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-md);
  border-color: var(--primary);
}
```

---

### 2. 命令面板 (Command Palette)

**技术实现**: 使用 `cmdk` 库

```tsx
<Command.Dialog open={open} onOpenChange={setOpen}>
  <Command.Input placeholder="搜索插件、命令..." />
  <Command.List>
    <Command.Group heading="插件">
      <Command.Item>
        <Icon />
        <div>
          <div>Python Runner</div>
          <div>运行 Python 脚本</div>
        </div>
        <kbd>↵</kbd>
      </Command.Item>
    </Command.Group>
    <Command.Group heading="命令">
      <Command.Item>
        <Icon />
        <span>打开设置</span>
        <kbd>⌘,</kbd>
      </Command.Item>
    </Command.Group>
  </Command.List>
</Command.Dialog>
```

**快捷键**:
- `⌘K` 或 `Ctrl+K` 打开
- `ESC` 关闭
- `↑↓` 导航
- `↵` 选择

---

### 3. Toast 通知系统

**技术实现**: 使用 `sonner` 库

```tsx
import { Toaster, toast } from 'sonner';

// 在 layout.tsx 中添加
<Toaster position="bottom-right" />

// 使用示例
toast.success('插件安装成功', {
  description: 'Python Runner v2.1.0',
  action: {
    label: '打开',
    onClick: () => router.push('/tools/python-runner')
  }
});
```

**类型**:
- ✓ Success - 绿色左边框
- ✗ Error - 红色左边框
- ⚠ Warning - 黄色左边框
- ⏳ Loading - 蓝色左边框

---

## 🛠️ UI 组件库选择

### 推荐方案: **Shadcn/ui + 自定义**

**理由**:
1. ✅ 无依赖 - 代码直接复制，完全可控
2. ✅ Tailwind 原生 - 与技术栈完美匹配
3. ✅ Radix UI 底层 - A11y 开箱即用
4. ✅ 易于定制 - 可修改源码
5. ✅ 现代设计 - 符合 2025 趋势

### 核心 Shadcn 组件

```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add dialog
npx shadcn@latest add dropdown-menu
npx shadcn@latest add tabs
npx shadcn@latest add badge
npx shadcn@latest add skeleton
npx shadcn@latest add command
npx shadcn@latest add sheet
```

### 额外依赖

| 库 | 用途 | 安装 |
|---|---|---|
| `sonner` | Toast 通知 | `pnpm add sonner` |
| `cmdk` | 命令面板 | `pnpm add cmdk` |
| `embla-carousel-react` | 轮播图 | `pnpm add embla-carousel-react` |
| `vaul` | 底部抽屉 | `pnpm add vaul` |

---

## 🎬 动效和交互

### 页面级动效

**路由切换动画**:
```tsx
<motion.div
  initial={{ opacity: 0, scale: 0.98 }}
  animate={{ opacity: 1, scale: 1 }}
  exit={{ opacity: 0, scale: 0.98 }}
  transition={{ duration: 0.2, ease: [0.4, 0, 0.2, 1] }}
>
```

### 组件级微交互

**卡片悬停**:
```tsx
<motion.div
  whileHover={{ y: -4, boxShadow: '...' }}
  whileTap={{ scale: 0.98 }}
  transition={{ type: 'spring', stiffness: 300 }}
>
```

**按钮点击**:
```tsx
<motion.button
  whileTap={{ scale: 0.95 }}
  whileHover={{ scale: 1.05 }}
>
```

### 加载状态

**骨架屏**（优于 Spinner）:
```tsx
<div className="animate-pulse">
  <div className="h-4 bg-neutral-200 rounded w-3/4 mb-2" />
  <div className="h-4 bg-neutral-200 rounded w-1/2" />
</div>
```

---

## 🚀 2025 趋势应用

### 1. AI 驱动的个性化

**实施场景**:
- 基于用户行为推荐插件
- 动态调整卡片排序
- 智能分类和标签

**技术实现**:
```tsx
const recommendedPlugins = useAIRecommendation({
  recentlyUsed: userHistory,
  categories: userPreferences,
  behavior: userBehavior
});
```

---

### 2. 3D 与沉浸式元素

**3D 悬浮卡片**:
```tsx
function TiltCard({ children }) {
  const x = useMotionValue(0);
  const y = useMotionValue(0);
  const rotateX = useTransform(y, [-100, 100], [10, -10]);
  const rotateY = useTransform(x, [-100, 100], [-10, 10]);

  return (
    <motion.div
      style={{ rotateX, rotateY, transformStyle: 'preserve-3d' }}
      onMouseMove={(e) => {/* 计算鼠标位置 */}}
    >
      {children}
    </motion.div>
  );
}
```

**玻璃态效果**:
```css
.glass-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(12px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.2);
}
```

---

### 3. 暗黑模式精细化

**深空黑调色板**:
```css
:root[class~="dark"] {
  --bg-primary: hsl(240, 10%, 4%);
  --bg-secondary: hsl(240, 8%, 8%);
  --primary: 210 100% 60%; /* 更亮 */
  --text-primary: hsl(0, 0%, 98%); /* 不是纯白 */
}
```

---

### 4. 大字体设计

**响应式超大标题**:
```css
.hero-title {
  font-size: clamp(3rem, 10vw, 7.5rem); /* 48px - 120px */
  font-weight: 800;
  line-height: 1.1;
  letter-spacing: -0.03em;
}
```

---

## 📅 实施路线图

### P0 - 核心基础（Week 1-2）

**Week 1**: 设计系统搭建
- [x] 创建设计文档
- [ ] 安装 Shadcn/ui
- [ ] 配置 Tailwind 主题
- [ ] 安装核心组件

**Week 2**: 核心页面布局
- [ ] 重构首页
- [ ] 重构插件市场
- [ ] 创建插件卡片组件

---

### P1 - 增强体验（Week 3-4）

**Week 3**: 关键组件
- [ ] 实现命令面板（cmdk）
- [ ] 添加 Toast 通知（sonner）
- [ ] 优化插件卡片动效
- [ ] 添加骨架屏加载

**Week 4**: 插件详情页
- [ ] Hero 区域
- [ ] 截图轮播
- [ ] Tab 切换
- [ ] 安装进度动画

---

### P2 - 高级功能（Week 5-6）

**Week 5**: 微交互优化
- [ ] 页面切换动画
- [ ] 3D 悬浮卡片
- [ ] 玻璃态效果
- [ ] 打字机效果

**Week 6**: 个性化和优化
- [ ] AI 推荐算法
- [ ] 主题定制器
- [ ] 虚拟滚动
- [ ] 性能监控

---

## 📚 参考案例

### 直接借鉴

| 产品 | 借鉴点 | 应用场景 |
|------|--------|---------|
| Arc Browser | 侧边栏设计 | 导航栏布局 |
| Raycast | 命令面板 | ⌘K 快速搜索 |
| Linear | 微动效 | 整体动画 |
| Vercel | 数据可视化 | Agent 监控 |
| App Store | 详情页 | 插件详情 |

### 创新差异化

1. **插件状态可视化** - CPU/内存实时显示
2. **Agent 中心** - Python/Node.js 独立模块
3. **GitOps 标记** - 显示 Git 仓库和更新频率
4. **权限透明化** - 安装前明确权限
5. **命令优先** - 所有操作通过 ⌘K

---

## 🎨 设计风格关键词

- 🎯 **科技感** - 霓虹渐变 + 玻璃态
- 🧘 **极简主义** - 大留白 + 清晰层级
- ⚡ **高性能** - 60fps 动画 + 骨架屏
- 🛡️ **可信赖** - 权限透明 + 状态可视化
- 🎨 **可定制** - 主题切换 + 密度调节

---

## 📝 设计决策总结

### 核心选择

1. ✅ **Shadcn/ui** - 保持设计独特性
2. ✅ **命令优先** - ⌘K 核心交互
3. ✅ **暗色优先** - 默认深色模式
4. ✅ **插件即主角** - 卡片突出特性
5. ✅ **拥抱 2025 趋势** - 大字体、微动效、AI 个性化

### 技术栈

- **UI 框架**: Shadcn/ui
- **动画**: Framer Motion
- **通知**: Sonner
- **命令面板**: cmdk
- **轮播**: Embla Carousel

---

**最后更新**: 2025-12-07
**维护者**: BoolTox Team
